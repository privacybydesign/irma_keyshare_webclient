# nodeLinker: node-modules keeps the classic node_modules/ layout so dev
# tooling, Vite, and the Dockerfile don't need any PnP-specific changes.
nodeLinker: node-modules

yarnPath: .yarn/releases/yarn-4.15.0.cjs
